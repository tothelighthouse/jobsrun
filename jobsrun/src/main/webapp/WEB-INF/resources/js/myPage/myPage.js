/*window.addEventListener("load",init,false);

function init(){
	
	 var id = document.getElementById('clientId').innerHTML;
	 console.log("id" + id);
	  evalBtn.addEventListener("click",function(e){
	  e.preventDefault(); //브라우저의 다른 행동을 막기위해  
	  location.href = getContextPath() + "/ratings/ratingForm/" + id;
	 },false);

	var id2 = document.getElementById('shooterID').innerHTML;
	console.log("id2" + id);
	 evalBtn2.addEventListener("click",function(e){
	 e.preventDefault(); //브라우저의 다른 행동을 막기위해  
	 location.href = getContextPath() + "/ratings/ratingForm/" + id2;
	},false); 
}*/